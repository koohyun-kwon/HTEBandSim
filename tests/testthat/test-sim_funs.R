test_that("coverage indicator function works", {
  # obs <- obs_gen(n = 1000, n.sim = 100, "AK", reg.spec = 2, M = 1, "het.AK", "norm", scale = 1/2, "beta")
  # x <- obs$x
  # y <- obs$y
  # y <- y[, 1]
  #
  # cb.res <- HTEBand::NpregBand(y, x, 1, 0.95, "H", n.eval = 50)
  # covind_gen(cb.res$eval, cb.res$cb.lower, cb.res$cb.upper, "AK", reg.spec = 2, M = 1)
})
